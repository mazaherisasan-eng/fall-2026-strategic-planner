(()=>{const DAY=86400000;function parse(s){if(!s)return null;const [y,m,d]=s.split('-').map(Number);return new Date(y,m-1,d)}function autoTerm(d=new Date()){const y=d.getFullYear(),m=d.getMonth()+1,day=d.getDate();if((m===12&&day>=15)||(m===1&&day<2))return{title:'Winter Break',year:m===12?y:y-1};if((m===1&&day>=2)||m<6||(m===6&&day<2))return{title:`Spring ${y}`,year:y};if((m===6&&day>=2)||m===7||(m===8&&day<15))return{title:`Summer ${y}`,year:y};return{title:`Fall ${y}`,year:y}}function monday(d){const x=new Date(d);x.setHours(0,0,0,0);x.setDate(x.getDate()-((x.getDay()+6)%7));return x}function cfg(){const a=autoTerm();state.academicCalendar=state.academicCalendar||{};const key=a.title.replace(/\s+/g,'_');state.academicCalendar[key]=state.academicCalendar[key]||{};return{a,key,t:state.academicCalendar[key]}}function title(){const {a,t}=cfg();return t.label||a.title}function apply(){const s=title();state.semesterTitle=s;document.title=`${s} Strategic Planner`;const h=document.querySelector('#login h2');if(h)h.textContent=`${s} Planner`;const b=document.querySelector('.brand');if(b)b.innerHTML=`${esc(s)}<br><span>Strategic Planner</span>`}function semesterWeek(){const {t}=cfg();if(!t.start)return null;const n=Math.floor((monday(new Date())-monday(parse(t.start)))/(7*DAY))+1;return n>=1&&n<=16?n:null}function addSettings(){if(!state||!Settings?.classList.contains('active')||Settings.querySelector('#academicCalendarCard'))return;const {t}=cfg();const card=document.createElement('div');card.className='card';card.id='academicCalendarCard';card.style.marginTop='16px';card.innerHTML=`<h2>Academic Calendar</h2><p class="muted">Each semester remains a separate 16-week planner. Set Week 1 manually; the planner keeps Week 1–16 as its core structure.</p><p><b>Current period:</b> ${esc(title())}</p><div style="display:grid;grid-template-columns:repeat(2,minmax(0,220px));gap:10px"><label>Week 1 starts<input id="termStart" type="date" value="${esc(t.start||'')}" style="display:block;width:100%;padding:9px;margin-top:5px"></label><label>Term ends<input id="termEnd" type="date" value="${esc(t.end||'')}" style="display:block;width:100%;padding:9px;margin-top:5px"></label></div><p class="muted">Automatic periods: Spring Jan 2–Jun 1 · Summer Jun 2–Aug 14 · Fall Aug 15–Dec 14 · Winter Break Dec 15–Jan 1. Manual dates control semester week numbering without changing the 16-week structure.</p><button class="primary" id="saveAcademicCalendar">Save semester dates</button>`;Settings.appendChild(card);card.querySelector('#saveAcademicCalendar').onclick=async()=>{t.start=card.querySelector('#termStart').value;t.end=card.querySelector('#termEnd').value;await save();render('Settings')}}function expose(){window.academicCalendar={activeTerm:title,semesterWeek,autoTerm,weeksPerSemester:16}}const oldRender=render;render=function(v){oldRender(v);apply();expose();if((v||document.querySelector('.view.active')?.id)==='Settings')setTimeout(addSettings,0)};setTimeout(()=>{if(!state)return;cfg();apply();expose();if(Settings?.classList.contains('active'))addSettings()},1800)})();